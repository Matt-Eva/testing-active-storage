class AvatarsController < ApplicationController
    # def create
    #     user = User.find(params[:user_id])
    #     user.avatar.attach(params[:file])
    #     url = url_for(user.avatar)
    #     img = user.avatar.attachment
    #     # puts user.avatar.methods
    #     puts user.avatar.attachment.methods
    #     path = rails_blob_path(user.avatar, disposition: "attachment")
    #     render json: {user: user, url: url, path: path, avatar: user.avatar, img: img}
    # end
end